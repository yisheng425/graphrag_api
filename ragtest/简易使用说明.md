uv venv
source .venv/bin/activate
uv sync

填一下seetings.yaml的内容，主要配置一下key
改一下输出目录
output:
  type: file # [file, blob, cosmosdb]
  base_dir: "output"

或者把原来的output文件夹改个名字，比如output2

uv run graphrag index --root ./ragtest --config ./ragtest/settings.yaml --verbose